Q1 Find the maximum and minimum element in the array?

ans -

class Codechef
{
    
    static void maxArray(int arr[]){
        int max = arr[0];        // 1st element ko max man liya
        for(int i = 1; i<arr.length; i++){    // loop 1 se chalaya
            if(arr[i] > max){    // check
                max = arr[i];   // update
            }
            
        }
          System.out.println(max);
    }
    
    
     static void minArray(int arr[]){
        int min = arr[0];         // 1st element ko min man liya
        for(int i = 1; i<arr.length; i++){
            if(arr[i] < min){
                min = arr[i];
                
            }
            
        }
        System.out.println(min);
    }
    
    
	public static void main (String[] args)
	{
		int arr [] = new int[]{11,34,3,4,5,6};
		maxArray(arr);    // function to find the max element in the array
		minArray(arr);    // function to find the max element in the array
		
	    
	}
}
